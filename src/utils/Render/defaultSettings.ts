// The renderer's default settings are the environment-independent base verbatim.
// This is only a placeholder shape — real values arrive via the `getSettings` IPC.
// Env-dependent defaults (clientId, $HOME paths) live in Utils/Main/defaultSettings.
export { BASE_DEFAULT_SETTINGS as DEFAULT_SETTINGS } from "Utils/Common/defaultSettings";
